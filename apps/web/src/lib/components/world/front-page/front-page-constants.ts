/**
 * Front-page constants used across helper modules.
 * Kept in one place so truncation budgets and defaults are easy to tune.
 */

export const FRONTPAGE_CONTEXT_MAX_CHARS = 2400;
export const FRONTPAGE_ENTITY_SNIPPET_MAX_CHARS = 900;
export const DEFAULT_RECENT_LIMIT = 6;
export const MIN_RECENT_LIMIT = 1;
export const MAX_RECENT_LIMIT = 24;
