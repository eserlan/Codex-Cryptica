# Quickstart: Staging Indicator

1. To test the staging indicator locally, run the app with the `staging` mode:
   ```bash
   VITE_APP_ENV=staging npm run dev --workspace=web
   ```
2. Verify the indicator is visible.
3. Check it on mobile viewport in DevTools.
