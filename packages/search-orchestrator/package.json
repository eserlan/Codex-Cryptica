{
  "name": "@codex/search-orchestrator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "bun test",
    "test:coverage": "bun test --coverage --coverage-reporter=text --coverage-reporter=lcov",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "eslint": "^10.7.0"
  },
  "dependencies": {
    "comlink": "^4.4.2",
    "dexie": "^4.4.2",
    "@codex/events": "workspace:*",
    "@codex/search-engine": "workspace:*",
    "@codex/vault-engine": "workspace:*",
    "schema": "workspace:*"
  }
}
